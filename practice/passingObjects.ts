let newObject:{myArray:number[]; myId?:number | null} = {
    myArray:[1,2,3,4]
}
const myId = "23";
newObject = {myArray:[1,2], myId:25 }
console.log(myObject);

//i want to add the failed devices and retrieve the devices that weren't disabled
